{
  "name": "meter-reading-ade-android",
  "version": "1.0.0",
  "private": true,
  "description": "ADE Meter Reading - Android app wrapper",
  "scripts": {
    "cap:add:android": "cap add android",
    "cap:copy": "cap copy android",
    "build:android": "cap copy android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/assets": "3.0.5",
    "@capacitor/cli": "8.5.2"
  }
}
